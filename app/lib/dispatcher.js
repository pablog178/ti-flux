/**
 * The dispatcher is the main mediator between the actions and stores, all stores 
 * @class Dispatcher
 * @singleton
 */

const LOG_TAG = '\x1b[35m' + '[dispatcher]' + '\x1b[39;49m ';

var Dispatcher = (function () {
	// +-------------------
	// | Private members.
	// +-------------------

	// +-------------------
	// | Public members.
	// +-------------------

	return {

	};
})();

module.exports = Dispatcher;